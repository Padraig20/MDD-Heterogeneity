[project]
name = "mdd-heterogeneity"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12.0, <3.13"
dependencies = [
    "bed-reader>=1.0.6",
    "enformer-pytorch>=0.8",
    "fastapy>=1.0.5",
    "numpy~=2.2.5",
    "pysam>=0.23.3",
    "scanpy~=1.11.1",
    "torch~=2.8.0",
    "transformers<5",
    "wandb>=0.23.1",
    "variantformer[notebook,test,marimo]",
]

[tool.uv.sources]
variantformer = { path = "variantformer", editable = true }
